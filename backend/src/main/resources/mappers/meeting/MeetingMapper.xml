<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.admin.the_climbing_night.meeting.mapper.MeetingMapper">
  <select id="hasMeeting" parameterType="com.admin.the_climbing_night.meeting.vo.HasMeetingVo" resultType="java.lang.String">
    SELECT
        id
    FROM
        meeting
    WHERE
        1 = 1
        AND hostDt = #{hostDt}
        AND climbingAreaFk = #{climbingAreaFk}
  </select>

  <insert id="insertMeeting" parameterType="com.admin.the_climbing_night.meeting.domain.req.InsertMeetingRequest">
    INSERT INTO meeting (
        id
        , name
        , memberFk
        , climbingAreaFk
        , createDt
        , hostDt
        , time
        , criticalMeetingYn
        , meetingStatusFk
    ) VALUES (
        #{id}
        , #{name}
        , #{memberFk}
        , #{climbingAreaFk}
        , #{createDt}
        , #{hostDt}
        , #{time}
        , #{criticalMeetingYn}
        , #{meetingStatusFk}
    )
  </insert>
</mapper>